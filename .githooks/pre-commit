#!/bin/sh

set -eu

repo_root=$(git rev-parse --show-toplevel)
cd "$repo_root"

if ! command -v node >/dev/null 2>&1; then
  echo "pre-commit: Node.js is required but was not found on PATH." >&2
  exit 1
fi

node scripts/check-assets.mjs staged
