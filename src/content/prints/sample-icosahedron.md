---
title: 'Sample Icosahedron'
summary: 'A procedurally generated placeholder model, used to demonstrate the interactive 3D preview before real prints are added.'
material: 'N/A — procedural sample, not a physical print'
tags:
  - sample
featured: false
order: 99
model:
  file: '/files/prints/sample-icosahedron.stl'
  sizeBytes: 1084
draft: false
placeholder: true
---

> [PLACEHOLDER] This entry has no physical print or photos yet — it exists to exercise the interactive .stl viewer end to end. Replace it with a real print (photos via `npm run media:import`, model via `public/files/prints/`) or remove it once real prints are added.

## About this preview

The model above is a plain twenty-face icosahedron, generated programmatically rather than printed. Drag to rotate, scroll or pinch to zoom, and use the download link to grab the raw `.stl`.
